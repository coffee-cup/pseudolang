Run `mise run tidy` to tidy the dependencies.

Run `mise run check` to check the code. Fix any issues and run the check again until it passes.

Once the test passes, commit ALL changes to the repository.
